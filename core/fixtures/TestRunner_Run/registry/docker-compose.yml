---

version: '3'
services:
  registry:
    environment:
      REGISTRY_STORAGE_DELETE_ENABLED: "true"
    image: registry:2.7.1
    ports:
    - "52854:5000"
    volumes:
    - "./config.yml:/etc/docker/registry/config.yml"
